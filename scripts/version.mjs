import * as Fs from "node:fs"
import * as Path from "node:path"

// This script is called after changeset version to perform any additional versioning tasks
// Currently it's a placeholder that can be extended as needed

console.log("✅ Version script completed") 
